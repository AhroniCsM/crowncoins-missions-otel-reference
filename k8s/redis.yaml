apiVersion: apps/v1
kind: Deployment
metadata:
  name: missions-redis
  namespace: default
  labels:
    app: missions-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: missions-redis
  template:
    metadata:
      labels:
        app: missions-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: missions-redis
  namespace: default
spec:
  selector:
    app: missions-redis
  ports:
    - port: 6379
      targetPort: 6379
